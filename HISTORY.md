# HISTORY — PAX 공공AX 사례 아카이브 연혁

주요 기능·운영 방식 변경·보고서 발행을 날짜순으로 기록한다. 일일 수집 결과는 비공개 `log.md`,
사례 단위 변경은 사이트 [변경 기록](https://hollobit.github.io/PAX/changelog.html)에 있다.
(2026-08-30 기준 커밋 279건, 사례 240건, 챔피언 109명)

## 2026-08

### 08-06 — 착수
- 설계서·구현 계획 작성(`docs/superpowers/specs/2026-08-06-pax-design.md`, `plans/2026-08-06-pax-archive.md`)
- 스키마 검증 → 익명화 게이트 → 병합(중복 제거·append-only) → site 동기화 파이프라인과 테스트 구축
- 정적 사이트 첫 구현, 일일 수집 절차서(`scripts/collect_prompt.md`), GitHub Pages 배포 워크플로

### 08-07 — 첫 수집과 사이트 골격 (커밋 48건)
- 잘못 커밋된 로컬 세션 상태(`.claude/`) 추적 제거·히스토리 정리, 익명화 게이트를 화이트리스트 방식으로 강화
- 카카오톡 수집 경로 확정(`kakaocli` 로컬 DB 읽기, Xcode 26.6 빌드) — 사례 4건부터 시작
- 선별 기준 확장: 링크가 공유된 개발 사례 포함, URL 없는 단순 소식 제외
- 썸네일 카드(`case_url`), 카드/목록 보기, 태그 필터·태그 뷰, 북마크, CSV/PDF 내보내기, 인기·신규 배지
- 메뉴: MCP·AI 도구, 변경 기록, 안내서·가이드라인, AX 자원·사업·기업 찾기, AI 정부실험실
- 하루 63건까지 확장(커뮤니티 도구·공공데이터 서비스·대시보드·로컬 도구 배치)

### 08-08 — 평가 대시보드
- Supabase 기반 전체 사용자 누적 북마크 집계 → 동적 인기 순위
- AX 수준 평가 대시보드(4축 방법론·성숙도 사다리·사례별 판정·위험도), AX 성숙도 개념 인포그래픽
- 사이트 제목 "모두의 공공AX 사례 아카이브" (76건)

### 08-11 ~ 08-15 — 배치 확장
- 첫 외부 기여 PR(#1): 광진구 AI 도구 6종 전면 투입 — 최초 AI-First(잠정) 판정
- 한국 공공데이터 MCP 생태계 배치 45건(129→139건), 법제처 AI 법령 비서, 개인정보보호법 안내 MCP
- 정부 공공 깃랩 배치 13건, 미러 저장소 양쪽 주소 병기 규칙(170건)
- 운영: 세션 크론 7일 만료로 자동 수집 누락 발생(08-15) → 재등록 절차 인지

### 08-16 ~ 08-17 — 챔피언 디렉토리
- 공공AX 챔피언 디렉토리 신설(공개 프로필 기반 83명), 동일 계정 자동 연결·공공 GitLab 우선, 미확인 사례 섹션
- AI 챔피언 기록 저장소 인증 배지·티어(GREEN/BLUE/BLACK) 필터, 검색에 챔피언 매칭

### 08-18 — 확장 전략 보고서
- PAX 확장 전략 다관점 분석(8개 관점) → 보고서 v2·PDF(공공AX 개념·10대 원칙·국가전략·해외 비교·추진단 연계·에이전트 환경)

### 08-19 ~ 08-20 — 라이선스·분류축
- 저장소 라이선스 태깅 118건(GitHub·공공 GitLab), merge 중복 검사에 URL 정규화 교차 비교
- 로드맵 Phase 0·1: 기관유형 10분류·`region`·`case_class`, 업무 분류축·동의어 검색, 실행환경 배지, 딥링크, robots·sitemap·llms.txt

### 08-21 — 관측소·격차 지도 (커밋 32건)
- 로드맵 Phase 2: 공공AX 지수·분기 스냅샷, 데이터 관측소, 지역 격차 지도(광역+중앙행정기관 34개), 전이 플레이북, 자가진단, 사례별 정적 상세 페이지
- 관측소: MCP 현황·이슈, 저장소 스타·유지보수 실측(111개), 라이선스 현황, 플랫폼별 스타 분리 집계
- 깃랩 스타 3+ 조사 → 신규 7건·`mirror_url` 필드 신설(195건), 주간 조사 절차 편입
- 보고서: 공공 깃랩 전략 비교(`docs/reports/2026-08-21-public-gitlab-oss-strategy.md`) 5장 완성, 확장전략보고서 PDF 개정판(33쪽)
- 운영: 크론 만료 자동 점검을 수집 절차 0단계에 편입

### 08-22 ~ 08-23 — 커뮤니티 지표·지역 정합
- 커뮤니티 활력 지표: 카카오 대화량(전 기간 백필 13,415건, 봇 집계 교차검증)·오픈톡 가입자 추이·Threads 관측 누적
- 생태계 연결 관측 설계 스펙·구현 계획(4층 구조 + GraphRAG, 구현 보류)
- MCP 생태계 4건으로 200건 돌파, 챔피언 101명
- 지역 백필 22건·지역 축 스코프 분류(전국 단위 vs 지역 미확인), 미상 구성 표

### 08-24 ~ 08-27 — 운영 안정화
- 첫 주간 점검(헬스체크·스타 재실측·카카오 백필), 정기 수집 하루 2회→3회(07:00/15:00/23:00), kakao 수집 창 잘림 자동 점검
- 신규: 보듬정착·재난문자 지도·ONE윈도우·HOP·UrbanLaw·Gov.AX Insight·IP-AX 3종(지식재산처 첫 관측) 등 (217건)
- 썸네일 캐시 무효화(`thumb_v` 스탬프)

### 08-28 — MCP 보안·안전 검증
- 설계 스펙·구현 계획(6축 프레임·responsible disclosure·오탐 체계), `check_mcp`·`build_mcp_review`·검증 매트릭스 페이지
- 전수 백필 67건(양호 10·주의 57·심각 0), LLM 감사 63건 완료
- vibecode-checker·하네스, 해외 참고(Orca·book-to-skill·MCP Inspector·Archify), 깃랩 스타 신규 3건 (231건)
- 참고 분류 계정 챔피언 제외 규칙(107명)

### 08-29 — 모델 관측·정책 보고서 (커밋 25건)
- 모델 정보 스키마 확장(`models_used`, 비LLM·BYO), 전 사례 모델 의존 백필 165건, 관측소 모델 현황 섹션(채택률·분포·패밀리 표)
- 전 페이지 섹션 딥링크(안정 id 40곳), JS·CSS 콘텐츠 해시 버전
- 보고서: AI 모델 사용 실태·정책 과제(`docs/reports/2026-08-29-ai-model-usage-analysis.md`) §1~12 — 중국 정책 참조, 에이전틱 소버린, 독자 프론티어 순환, 크리티컬 영역 전략, 안전 기술×조달 연계; 멀티 페르소나 리뷰·팩트체크 51건 정정
- 신규: 김창일 연작·Attendance Checker·Capptivo·G2B Masters (237건)

### 08-30
- 정기 수집: HRMCP·인사스캔·국산 경량모델 조례 실측(첫 국산 오픈웨이트 관측) 등 3건, HRMCP MCP 6축 검증 (240건)
- 저장소 문서 정비: `AGENTS.md`(에이전트 작업 지침)·`HISTORY.md`(이 파일)·`CLAUDE.md` 신설, README 현행화(하루 3회·카카오 가동·페이지 안내)
- 운영: 커뮤니티 지표 원장(`data/community_stats.json`)을 정기 수집 커밋 경로에 편입, `.DS_Store` ignore
- 운영: 사례 단건 등재·보강 스킬 `pax-register` 신설(로컬 `.claude/skills`, 평가 항목 추가 등 절차서 밖 단계를 체크리스트화)

## 2026-09

### 09-01 ~ 09-03 — 공공 깃랩 정기 조사 체계화
- 주간 깃랩 조사 범위를 스타순 상위 200개로 확대, 순수 신규는 승인 대기 없이 자동 등재하는 규칙 확정
- 깃랩 사례 중복·복제본 점검을 상시 규칙으로 편입(같은 이름 저장소 경고를 `pax-register` 사전 검사에 내장)

### 09-04 ~ 09-06 — 카카오 수집 무중단화
- 수집 워치독 `scripts/kakao_watchdog.sh` + launchd(30분 주기): 전 대화방 최신 시각이 45분(심야 240분) 이상 정체하면 앱 재실행, 지속 시 macOS 알림
- 정상 판정 때마다 2일 창을 자동 덤프해 세션·크론과 무관하게 대화 원장이 끊기지 않도록 함
- 전체 디스크 접근 권한 등록으로 권한 승인 창 없이 무인 실행(도구 업그레이드 시 재등록 필요)

### 09-07 ~ 09-08 — 챔피언 인증 매핑
- 공공 AI 챔피언 기록 저장소(aichampion.site)·언론 보도 기반 인증 챔피언 매핑, 인증 티어 표시
- 표시 이름에서 기관·지역·직급 토큰을 분리하는 일반 규칙 도입(개별 예외 처리 대신 규칙으로 교정)

### 09-09 — PAX 현황판
- 관측소 첫머리에 9개 핵심 지표(사례 총계·MCP 비율·챔피언·오픈톡 가입자·국산 모델 채택률·로컬 오픈웨이트 실행률·GitHub/공공 깃랩 저장소 규모·깃랩 라이선스 미표시)를 모은 계기판 추가
- 비율은 링 게이지, 추세가 있는 규모는 추이선, 비교 가능한 규모는 쌍 막대. 별 합계는 척도 차가 커 같은 축에 두지 않고 값으로 병기
- 지표별 카테고리 색 대신 단일 강조색 + 상태색(주의·낮음 텍스트 배지 병기)만 사용, 전부 CSS 토큰이라 다크모드 자동 적용
- 사이트 자산 캐시 스탬프 갱신 스크립트 `scripts/stamp_assets.py` 도입(에셋 수정 후 필수 단계)
- 현황판에 분류 구성 패널 3종 추가 — 기관 유형·업무 분류·공공데이터 도메인별 MCP. 항목이 10~20개라 카테고리 색 대신 순위 막대(크기 한 가지 뜻)로 표현
- 도메인 분류는 접근성 매트릭스의 `DATA_DOMAINS` 정의를 공유 함수로 추출해 재사용 — 표와 현황판이 어긋날 수 없게 함. 미관측 도메인(예산·재정, 복지·안전망, 관세·무역)을 함께 표기
- 공공 깃랩 정기 조사 40건 추가 (총 375건, 챔피언 171명)

### 09-09 — 카카오 수집 무인화 2차
- kakaocli의 구조를 실측 확정: 앱이 관리하는 암호화 SQLite를 읽기만 하며 서버 통신 경로가 없다.
  앱을 닫으면 DB와 파일 mtime이 정지하고(3분 관측) 읽기 명령만 동작 — 앱이 사실상 동기화 데몬이다
- 워치독을 3층 구조로 확장: ① 프로세스 감시(종료 시 숨김 모드 즉시 재기동) ② 로그인 감시(명시적 로그아웃만 알림)
  ③ 기존 정체 감시. launchd 주기를 30분 → 10분으로 단축해 사용자가 앱을 닫아도 10분 안에 복구
- 동시 실행 락 추가 — 주기 실행과 수동 실행이 겹쳐 기동 중인 앱을 로그아웃으로 오판하던 헛알림 제거
- 기동 실패 가드가 성공한 기동까지 기억해 정당한 재기동을 막던 결함 수정(실패에만 적용)

### 09-10 — 카카오 재실행 경로 결함 수정
- 11시간(03:27Z~14:35Z) 동기화 정체 발생. 워치독은 재실행을 여섯 차례 기록했으나 실제로는 앱이 죽어 있었다
- 원인: 정체 대응의 종료·기동에 확인 절차가 없었다. `osascript quit`는 앱이 굳으면 애플 이벤트 타임아웃까지
  매달리는데 고정 5초만 기다린 뒤 `open`을 던져, 종료 중인 인스턴스를 붙잡아 기동이 흘러갔다.
  `open`의 종료코드는 요청 접수일 뿐인데 이를 기동 성공으로 로그에 남겨 실패가 가려졌다
- 종료를 프로세스 소멸로 확인하고(안 내려가면 신호로 마무리) 기동도 `pgrep`으로 확인하도록 수정.
  동기화 확인 창을 2분 → 4분으로 넓히고 재실행 가드를 90분 → 45분으로 줄여 공백 상한을 낮춤
- 정체 구간의 메시지는 앱 복귀 후 전량 backfill돼 대화 원장 손실은 없었다

### 09-11 — 첫 화면 분야 필터와 쪽 나누기
- 첫 화면에 **분야** 칩 줄 신설(국방·병무·교육·학교·개인정보·보안·소방·재난·의료·복지·특허·제도).
  업무(기능) 축과 독립한 두 번째 축이라 `task_category`에 섞지 않았다 — 단일값 필드라 한 사례가
  여러 분야에 걸치는 것을 담을 수 없고, 기존 10분류를 재배정하면 업무 축이 무너진다.
  원장 스키마는 그대로 두고 제목·태그·요약 키워드로 판정하며, 업무 필터와 AND로 결합된다
- 관측소 현황판에 **분야별 구성** 패널 추가(기관 유형·업무 분류에 이어 세 번째 구성 패널).
  분야 정의를 `site/case-domains.js` 한 곳으로 빼 첫 화면 칩과 현황판이 같은 파일을 쓴다 —
  정의를 양쪽에 복사해 두면 같은 사이트에서 다른 숫자가 나온다.
  한 사례가 여러 분야에 걸치므로 비율 합이 100%를 넘고, 어느 분야에도 안 걸린 사례 수를 함께 밝힌다
- **분야 판정에 기관명과 만든 사람의 소속을 더했다**(사용자 지시). 도구 설명만 보면 어디에도
  안 걸리지만 어느 현장에서 나왔는지가 분야를 말해 주는 사례가 많다 — 교육청 실무자의 범용 문서
  도구, 병무청 실무자의 일정 도구가 그렇다. 소속은 챔피언 원장의 공개 프로필에서 온다
- 이 변경으로 축의 뜻이 '도구가 다루는 분야'에서 **'도구가 나온 현장의 분야'까지**로 넓어졌다.
  패널 주석에 판정 근거를 적어 둔다. 교육·학교 28→42, 국방·병무 7→15, 소방·재난 10→13,
  의료·복지 14→16, 어느 분야에도 안 걸린 사례 234→213건
- 소속을 엮는 방법도 공유 파일에 넣어 두 화면이 같은 결과를 낸다(관측소가 챔피언 원장을 함께 읽는다)

### 09-11 — 현황판 증감 표시
- 각 지표 이름 옆에 **직전 기록 대비 증감**을 붙였다. 저장소에 지표 이력이 없어
  `scripts/build_dashboard_history.py`로 일자별 원장을 새로 두고 정기 수집에 편입했다
- 두 갈래로 채운다 — 사례 원장에서 되살릴 수 있는 지표(사례 수·MCP·모델 채택률·저장소 수·라이선스)는
  `collected_at` 기준으로 그날까지 등재된 사례만 모아 다시 계산(**복원**)하고, 되살릴 수 없는
  지표(챔피언 수·저장소 스타)는 오늘부터 실측을 적는다(**관측**). 가입자는 커뮤니티 원장에 이미 일자별로 있다
- 복원값의 한계를 항목마다 `source`로 남긴다 — 지금 원장으로 되돌아본 값이라 그날 화면의 숫자와
  다를 수 있다(나중에 지운 중복·고친 분류가 소급된다). 배지 설명에도 '복원값 포함'을 적는다
- **색으로 잘잘못을 말하지 않는다**. 라이선스 미표시율처럼 오르는 게 나쁜 지표가 있어,
  방향은 기호(▲▼)로만 알리고 배지 색은 한 가지 중립색을 쓴다
- 비교할 이전 값이 없거나 변화가 없으면 배지를 붙이지 않는다 — '0'이나 '—'를 채우면
  변화가 없다는 뜻인지 자료가 없다는 뜻인지 구분되지 않는다

### 09-11 — 정기 수집 파이프라인 정리
- 수집 절차가 '수집 0건이면 4~6단계를 건너뛴다'로 되어 있어, 신규 사례가 없는 회차에는
  공유 동영상·뉴스·대화량 갱신까지 함께 건너뛸 수 있었다. 실제로는 사례가 안 늘어도 공유물은 쌓인다
- 5단계를 둘로 갈랐다 — **5-A(신규 사례가 있을 때만)**: 병합·라이선스·게시·챔피언·사례 페이지·
  MCP 검증·썸네일·평가 항목. **5-B(매 회차)**: 커뮤니티 지표·공유 동영상·공유 뉴스·지수·현황판 이력
- 5-B는 raw 아카이브 전체를 다시 훑는 집계라 이전 회차 누락분이 여기서 메워진다.
  `build_dashboard_history.py`는 지수·챔피언 뒤에 두어야 그날 값이 제대로 기록된다

### 09-11 — 공유 목록 검색
- 공유 동영상·공유 뉴스에 키워드 검색을 넣었다. 대상은 제목과 채널·매체이며, 띄어쓴 낱말은
  **모두** 들어 있어야 걸린다(좁혀 가며 찾는 쪽이 목록에서 쓸모 있다)
- 검색어는 주소(`?q=`)에 남아 그대로 공유된다. 뉴스는 검색을 좁히면 보던 쪽이 사라지므로 첫 쪽으로 돌아가고,
  분류 칩 수도 검색 결과 안에서 센다 — 3건만 남았는데 칩이 5를 가리키면 어긋나 보인다
- 결과가 없을 때는 빈 상태 문구를 띄우고, 검색 중이 아닐 때는 결과 수를 감춘다

### 09-11 — 공유 뉴스 판별 보강(정기 수집 중 발견)
- `og:type`도 발행시각도 달지 않는 매체가 있어 실제 기사가 빠지고 있었다(헬스조선 등).
  **매체 이름이 있고 주소가 기사 모양이면** 받아들이도록 넓혔다
- EUC-KR로 내보내는 매체의 제목이 깨진 채 실렸다 — 문서가 선언한 인코딩을 따르도록 고치고,
  대체문자가 섞인 제목은 아예 싣지 않는다
- 기사가 내려간 자리의 안내 문구('언론사의 요청으로 삭제된 기사입니다')가 제목으로 들어왔다 — 제외
- 제목 끝 매체명은 구분자가 매체마다 달라(`ㅣ`·`:`·없이 띄어쓰기만) 남아 있었다 — 함께 처리
- **실패를 영영 기억하던 캐시를 고쳤다**: 그때 막혔던 곳이 되살아나도 다시 못 보게 되므로,
  실패에 날짜를 남겨 이레가 지나면 다시 두드린다 (413→417건)
- 목록 정렬이 실행마다 흔들려(동점 항목의 순서가 집합 순회 순서를 따랐다) 내용이 같아도 파일이
  통째로 다시 쓰였다 — 매 회차 커밋에 수백 줄 잡음이 남는다. 주소·id를 마지막 정렬 기준으로 두어 고정했다

### 09-11 — 운영 사이트 우선 표시
- **운영 사이트가 있으면 저장소보다 먼저 보여 준다**(사용자 지시). 사람이 사례를 알아보는 것은
  돌아가는 화면이지 코드 목록이 아닌데, 7건이 깃허브·깃랩 화면으로 보이고 있었다
- 원인은 규칙이 슬롯 순서에 매여 있던 것 — `case_url`이 있으면 무조건 그것을 썼다. 어느 슬롯에
  서비스가 들어갈지는 등재 때 사정에 따라 달라서, 같은 성격의 사례가 어떤 것은 서비스로 어떤 것은
  저장소로 보였다. 이제 세 슬롯을 모두 보고 **저장소가 아닌 주소를 먼저** 고른다
- 규칙을 `scripts/pax/urls.py` 한 곳에 두고 썸네일 생성·사례 상세 페이지·카드 링크가 함께 쓴다
  (사이트 쪽은 같은 규칙을 `app.js`에 옮겨 적었다 — 한쪽만 고치면 썸네일과 링크가 다른 곳을 가리킨다)
- `github.io`·`vercel.app` 같은 배포 주소는 호스팅이 깃허브여도 서비스로 본다.
  게시물 주소(Threads 등)는 사례의 대상이 아니라 출처이므로 표시 대상에서 뺀다
- 등재 절차서와 `pax-register` 스킬에서 '슬롯 순서를 맞추라'는 옛 지침을 걷어 냈다 —
  아는 주소를 빠짐없이 채우는 것이 할 일이고, 무엇을 보여 줄지는 규칙이 정한다
- 검색 칸을 넣으면서 상단 상자가 눌려 '정렬' 라벨이 두 줄로 접혔다. 원인은 긴 요약 문구가
  같은 줄의 `auto` 열을 넓게 차지한 것 — 요약을 아랫줄로 내리고 검색 칸 상한을 20rem → 14rem으로 줄였다.
  라벨에는 `white-space: nowrap`을 걸어 좁아져도 라벨 대신 옆 칸이 줄게 했다
- 정렬 칸도 검색 칸과 같은 세로 구조(라벨 위·내용 아래)로 맞췄다 — 한쪽만 가로로 두니
  칩과 입력창 높이가 어긋나 보였다
- 사례 목록을 **100건씩 쪽 나누기**(카드·표 보기). 필터·정렬·보기가 바뀌면 1쪽으로 돌아가고,
  `page` 파라미터로 공유되며, 사례 깊은 링크는 그 사례가 있는 쪽으로 자동 이동한다.
  내려받기는 보이는 쪽이 아니라 걸러진 전체를 담는다
- **썸네일 지연 로딩이 실제로는 꺼져 있던 결함 수정**: `img.src`를 `loading='lazy'`보다 먼저
  대입해 lazy 지정이 무시되고 있었다(대입 시점에 로딩 방식이 확정된다). 첫 화면에서 썸네일
  382장을 모두 내려받던 것이 화면에 들어온 것만 받도록 바뀌었다 — 첫 화면 지연의 주된 원인이었다.
  카드 DOM 생성 자체는 382장에도 15ms 안팎이라 병목이 아니었다(실측)

### 09-11 — 공유 동영상
- 오픈채팅·Threads에서 공유된 동영상을 모은 **공유 동영상** 메뉴 신설(변경 기록 왼쪽).
  수집 원본을 다시 훑어 52편을 복원했고, 최근 공유순·많이 공유된 순으로 볼 수 있다
- 공개하는 것은 영상 자체의 정보(제목·채널·공유 시점·공유 횟수)뿐이다. 누가 어떤 말과 함께
  올렸는지는 데이터에 담지 않는다 — 원문·닉네임 비공개 규칙을 수집 단계에서 지킨다
- 공유 횟수는 **메시지 id 기준**으로 센다. 워치독이 2일 창을 30분마다 덤프하므로 파일 단위로 세면
  한 번 올라온 영상이 열 몇 번 추천된 것처럼 부풀려진다(실제 52편·67회, 재공유는 12편)
- 제목은 oEmbed로 한 번 받아 비공개 경로에 캐시하고, 조회되지 않는 영상은 목록에서 뺀다(죽은 링크 방지)

### 09-11 — 공공 깃랩 정기 조사(19건 등재, 총 400건)
- 스타순 상위 200개 대조 — 기등재 140건, 미등재 60건. 미등재분은 README를 전수 열어 실체를 확인했다
- 이전 조사가 저장소 `description` 빈 값으로 걸러 낸 것 중 실체 있는 저장소가 있었다.
  깃랩 기본 템플릿 README(6천 자 안팎의 안내 문구)와 실제 문서를 길이·내용으로 갈라 다시 판정했다
- 등재 19건: 안심학교 MAP·서울교육 출장 여비 지도·교육행정 코퍼스·PDF 도깨비 방망이·Legal Reviewer·
  KSPO 데이터 지도·PII Cleaner·세차내역 자동입력·교육행정 질의응답 검색·자치법규 정비 대시보드·
  NCS JD·SlideForge·myAI·DeskNote·Alarm·교사 개발자 연수 시안·행사 동의 시안·광주지명필터·PDF→MD 변환기
- 모델 의존도는 저장소별로 근거를 확인해 정했다 — 국산 독자모델 1, 로컬 오픈웨이트 3, 상용 API 2,
  모델 중립 3, 나머지는 비LLM. 사천만 매개변수 개인정보 탐지 모형처럼 언어모델이 아닌 것은
  채택률 분모에 넣지 않는다
- **중복 등재 정리**: AI지원담당이 Threads 경로와 깃랩 경로로 두 번 올라가 있었고, 깃랩 쪽 주소가
  중간에서 잘려 링크가 끊겨 있었다. 하나로 합치고 온전한 주소를 미러로 병기했다.
  레스큐79도 깃랩 미러를 병기했다

### 09-11 — 공유 뉴스
- 오픈채팅·Threads에서 공유된 뉴스 기사를 모은 **공유 뉴스** 메뉴 신설(공유 동영상 옆).
  388건·매체 178곳, 최근 공유순·많이 공유된 순·기사 발행순으로 볼 수 있고 100건씩 쪽을 나눈다
- 기사 판별을 매체 목록으로 하지 않는다 — 실제 공유된 호스트가 300종을 넘고 지역지·전문지가
  장기 꼬리를 이뤄 손으로 관리하면 반드시 빠진다. 대신 **페이지를 열어 기사 표지를 본다**
  (`og:type=article` 또는 발행시각 메타). 죽은 링크와 접힌 링크도 같은 자리에서 걸러진다
- 공개하는 것은 기사 자체의 정보(제목·매체·발행일·공유 시점·공유 횟수)뿐이다
- 첫 판에서 정부 도메인(`.go.kr`·`korea.kr`)을 통째로 배제했다가 걷어 냈다 — 정책브리핑은
  `article:published_time`을 제대로 달아 기사로 판별되고, 데이터 포털·업무 시스템은 그 표지가 없어
  어차피 걸러진다. 같은 규칙에 앵커가 없어 언론사 `thekorea.kr`까지 함께 배제되던 것도 함께 고쳤다(388→399건)
- 이어서 **기관 보도자료와 공고·안내를 목록에 넣었다**(사용자 지시, 399→407건). 정부 게시판은
  `og:type`도 발행시각도 달지 않아 기사 판별로는 잡히지 않으므로, 제목에 남는 게시판 표지
  (보도자료·정책뉴스·공모·공고·자료 등)로 가른다. 포털 메인과 데이터셋 화면에는 그 표지가 없어
  자연히 빠지고, 목록 화면은 표지만 있고 머리기사가 없어 길이에서 걸린다.
  제목이 빵부스러기뿐인 기관 페이지(`HOME > 알림마당 > 공지사항`)는 메뉴 이름만 남으므로 제외한다
- 목록에 분류(기사·보도자료·공고·안내)를 표시하고 걸러 볼 수 있게 했다. 같은 글이 모바일·단축·게시판
  파라미터 차이로 여러 건이 되던 것은 제목과 매체가 같으면 한 글로 합친다(공유 횟수는 더한다)
- **첨부 문서는 싣지 않기로 했다**: 공유된 PDF 56건은 링크 없이 파일명만 남고, 경영전략·의견수렴
  안건 같은 내부 문건이 섞여 있어 제목 공개 자체가 원문 비공개 규칙에 걸린다

### 09-11 — 공유 동영상 커서 미리보기
- 썸네일에 커서가 머물면 그 자리에서 음소거 재생. 목록을 가로지르는 커서에 플레이어가 줄줄이
  붙지 않도록 450밀리초 머문 뒤에야 띄우고, 한 번에 하나만 남긴다
- 쿠키를 덜 남기는 도메인으로 임베드하며, 카드를 열어 볼 때가 아니라 **커서가 머물 때만** 불러온다
- 손가락 입력 기기와 모션 최소화를 켠 이용자에게는 켜지 않는다. 재생 중에도 카드를 누르면
  유튜브로 가도록 링크를 미리보기 위에 덮었고, 카드를 `article`로 바꿔 링크가 겹치지 않게 했다
